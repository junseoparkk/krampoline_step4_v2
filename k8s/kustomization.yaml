namespace: default
resources:
  - nginx.yaml
  - mysql.yaml
  - backend.yaml
  - frontend.yaml
configMapGenerator:
  - name: nginx
    files:
      - configs/default.conf
  - name: init-db
    files:
      - configs/init.sql
secretGenerator:
  - name: my-secret-file
    files:
      - configs/secret.json
